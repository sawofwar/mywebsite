<div class="hero">
  <h1 class="hero__heading">Hello, my name is Constantine</h1>
  <p class="hero__subtext">
    Aspiring <strong>web-developer and QA engineer</strong> based in Tbilisi, Georgia.
  </p>
</div>

<style>
  .hero {
    height: 95vh;
    min-height: 600px;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    padding: 72px;
    color: #f8f9fa;
    grid-row: 1;
  }

  .hero::after {
    content: '';
    background-image: url('../src/images/background-1.avif');
    background-size: 100vw;
    background-overflow: hidden;
    position: absolute;
    filter: brightness(0.1);
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    grid-row: 1;
  }

  h1 {
    font-family: merriweather;
    font-size: 52px;
    letter-spacing: -2px;
    margin-bottom: 24px;
    z-index: 1;
  }

  .hero__subtext {
    z-index: 1;
    font-size: 18px;
  }

  @media only screen and (max-width: 600px) {
    h1 {
      font-size: 36px;
      letter-spacing: -0.8px;
    }
  }

  @media only screen and (min-width: 900px) {
    .hero {
      margin-left: 72px;
    }
  }
</style>
